@model ThuonghieuModel

<h1>Create</h1>

<h4>Thương hiệu</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-action="Create">

            <div class="form-group">
                <label asp-for="@Model.Tenthuonghieu" class="control-label">Tên thương hiệu</label>
                <input asp-for="@Model.Tenthuonghieu" type="text" class="form-control" />
                <span asp-validation-for="@Model.Tenthuonghieu" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
